<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecommerce.repository.OrderMapper">

    <resultMap id="orderMap" type="com.ecommerce.domain.Order">
        <id property="id" column="id"/>
        <result property="orderNo" column="order_no"/>
        <result property="userId" column="user_id"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="status" column="status"/>
        <result property="address" column="address"/>
        <result property="remark" column="remark"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="username" column="username"/>
        <collection property="items" column="id" javaType="java.util.ArrayList"
                    ofType="com.ecommerce.domain.OrderItem"
                    select="com.ecommerce.repository.OrderItemMapper.findByOrderId"/>
    </resultMap>

    <insert id="insert" parameterType="com.ecommerce.domain.Order" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (order_no, user_id, total_amount, status, address, remark)
        VALUES (#{orderNo}, #{userId}, #{totalAmount}, #{status}, #{address}, #{remark})
    </insert>

    <select id="findById" resultMap="orderMap">
        SELECT o.*, u.username
        FROM orders o
        LEFT JOIN user u ON o.user_id = u.id
        WHERE o.id = #{id}
    </select>

    <select id="findByUserId" resultMap="orderMap">
        SELECT o.*, u.username
        FROM orders o
        LEFT JOIN user u ON o.user_id = u.id
        WHERE o.user_id = #{userId}
        ORDER BY o.create_time DESC
    </select>

    <select id="findAll" resultMap="orderMap">
        SELECT o.*, u.username
        FROM orders o
        LEFT JOIN user u ON o.user_id = u.id
        ORDER BY o.create_time DESC
    </select>

    <update id="updateStatus">
        UPDATE orders SET status = #{status}, update_time = CURRENT_TIMESTAMP WHERE id = #{id}
    </update>
</mapper>
